# Node
node_modules/
npm-debug.log
yarn-error.log

# Entorno
.env
.env.local

# Credenciales y secretos
credenciales-google.json

# Archivos de SO
.DS_Store
Thumbs.db

# Clasp local (archivos dinámicos si se requiere)
.clasp.json
*.clasp.local.json
crm-webapp/.clasp.prod.json

# Bot Data (legacy whatsapp-web.js)
agent-bot/baileys_auth_info/
agent-bot/bot_sessions/
agent-bot/qr.png
agent-bot/pairing_qr.png
agent-bot/.wwebjs_cache/
agent-bot/error.txt

# Evolution API (datos locales de Docker)
evolution-api.env

# Docker volumes (si se persisten localmente)
postgres_data/
redis_data/

# Deploy (archivos de producción con datos sensibles)
deploy/
beautyos-landing/hostinger/*.zip

# Clasp backup
*.backup

# Retry queue persistence (datos sensibles de leads pendientes)
agent-bot/data/

# Configuración privada por instancia
tenants.json
local-crm/config.json
_clientes/
ops/
docs/privado/

# Configuración privada del proxy Hostinger
beautyos-landing/hostinger/beautyos/api/config.php

# Artefactos locales y previsualizaciones
.codex-preview/
tmp-*
tmp/
temp/
